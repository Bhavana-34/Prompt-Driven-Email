# Example secrets file for local testing (DO NOT commit real keys)
# Copy to `.streamlit/secrets.toml` and fill values for local development.

OPENAI_API_KEY = "sk-REPLACE_ME"
OPENAI_MODEL = "gpt-4"

# If you want to test IMAP ingest locally, you can add these (use app password or OAuth token):
# IMAP_SERVER = "imap.gmail.com"
# IMAP_USER = "you@example.com"
# IMAP_PASS = "APP_PASSWORD"
